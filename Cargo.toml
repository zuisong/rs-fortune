[package]
edition = "2021"
name = "rs-fortune"
version = "0.2.0"

[dependencies]
rand = "0.8.5"

# https://github.com/johnthagen/min-sized-rust
[profile.release]
codegen-units = 1 
lto = true 
opt-level = "z" # Optimize for size.
panic = "abort" 
strip = true
